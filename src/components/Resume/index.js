import React from 'react';

function Resume() {
  return (
    <section id="resume" class="resume">
    <h2 class="section-title primary-border">Resume</h2>
    <p>
      This is resume section placeholder where I'd like to keep my CV.
    </p>
  </section>
  );
}

export default Resume;